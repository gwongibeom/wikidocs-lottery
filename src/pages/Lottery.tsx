import Button from '../components/Button'

const Lottery = () => {
  return (
    <Button
      title={'추첨'}
      onClick={() => console.log('POSITIVE')}
      style='POSITIVE'
    />
  )
}

export default Lottery
